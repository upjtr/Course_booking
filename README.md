# 📚 ระบบจองคอร์สเรียน (Course Booking System)

ระบบจองคอร์สเรียนออนไลน์ พร้อมปฏิทินแสดงการจอง และบันทึกข้อมูลลง Google Sheets

## ✨ Features

- 🎓 แสดงรายการคอร์สเรียนแบบการ์ด
- 📝 ฟอร์มกรอกข้อมูลจองเรียน (ชื่อ, อีเมล, เบอร์โทร, คอร์ส, วันที่, เวลา)
- 📅 ปฏิทินแสดงวันที่มีการจอง
- 💾 บันทึกข้อมูลลง Google Sheets อัตโนมัติ
- 📱 Responsive Design รองรับทุกขนาดหน้าจอ
- 🎨 ออกแบบสวยงามด้วย Tailwind CSS

## 📁 โครงสร้างไฟล์

```
Project_2/
├── index.html              # หน้าเว็บหลัก
├── app.js                  # JavaScript สำหรับ logic ทั้งหมด
├── google-apps-script.js   # โค้ดสำหรับ Google Apps Script
└── README.md               # คู่มือการใช้งาน
```

## 🚀 การ Deploy ขึ้น GitHub Pages

### ขั้นตอนที่ 1: สร้าง GitHub Repository

1. ไปที่ [GitHub.com](https://github.com) และ Login
2. กดปุ่ม **New** หรือ **+** เพื่อสร้าง Repository ใหม่
3. ตั้งชื่อ Repository เช่น `course-booking`
4. เลือก **Public**
5. กด **Create repository**

### ขั้นตอนที่ 2: Push โค้ดขึ้น GitHub

```bash
# เปิด Terminal ในโฟลเดอร์ Project_2

# Initialize git
git init

# เพิ่มไฟล์ทั้งหมด
git add .

# Commit
git commit -m "Initial commit - Course Booking System"

# เชื่อมต่อกับ GitHub (เปลี่ยน USERNAME เป็น username ของคุณ)
git remote add origin https://github.com/USERNAME/course-booking.git

# Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

### ขั้นตอนที่ 3: เปิดใช้งาน GitHub Pages

1. ไปที่ Repository บน GitHub
2. กด **Settings** (ด้านบนขวา)
3. เลือก **Pages** จากเมนูด้านซ้าย
4. ที่ **Source** เลือก **Deploy from a branch**
5. เลือก Branch: **main** และ Folder: **/ (root)**
6. กด **Save**
7. รอสักครู่ แล้วจะได้ URL: `https://USERNAME.github.io/course-booking/`

## 📊 การเชื่อมต่อ Google Sheets

### ขั้นตอนที่ 1: สร้าง Google Sheet

1. ไปที่ [Google Drive](https://drive.google.com)
2. กด **New** → **Google Sheets**
3. ตั้งชื่อ Sheet เช่น "Course Bookings"

### ขั้นตอนที่ 2: สร้าง Google Apps Script

1. ที่ Google Sheet ที่สร้าง กด **Extensions** → **Apps Script**
2. ลบโค้ดเดิมทั้งหมด
3. คัดลอกโค้ดจากไฟล์ `google-apps-script.js` ไปวาง
4. กด **Save** (Ctrl+S)
5. ตั้งชื่อ Project เช่น "Course Booking API"

### ขั้นตอนที่ 3: Deploy Google Apps Script

1. กด **Deploy** → **New deployment**
2. กดไอคอนฟันเฟือง ⚙️ → เลือก **Web app**
3. ตั้งค่า:
   - **Description**: Course Booking API
   - **Execute as**: Me
   - **Who has access**: Anyone
4. กด **Deploy**
5. กด **Authorize access** (อนุญาตให้เข้าถึง)
6. เลือก Google Account ของคุณ
7. กด **Advanced** → **Go to [Project Name] (unsafe)**
8. กด **Allow**
9. **คัดลอก Web app URL** ที่แสดง

### ขั้นตอนที่ 4: ใส่ URL ในเว็บ

1. เปิดไฟล์ `app.js`
2. หา บรรทัด:
   ```javascript
   const GOOGLE_SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE';
   ```
3. เปลี่ยนเป็น URL ที่คัดลอกมา:
   ```javascript
   const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/xxx.../exec';
   ```
4. Save และ Push ขึ้น GitHub อีกครั้ง

## 📝 โครงสร้างข้อมูลใน Google Sheets

| Timestamp | ชื่อ-นามสกุล | อีเมล | เบอร์โทร | คอร์สที่เรียน | วันที่เรียน | เวลาเรียน | หมายเหตุ |
|-----------|------------|------|---------|-------------|-----------|---------|---------|
| 2026-02-06 10:30 | สมชาย ใจดี | somchai@email.com | 081234xxxx | Basic Programming | 2026-02-10 | 09:00 - 10:30 | - |

## 🎨 การปรับแต่ง

### เพิ่ม/แก้ไขคอร์สเรียน

แก้ไขใน `app.js` ที่ array `courses`:

```javascript
const courses = [
    {
        id: 1,
        name: 'ชื่อคอร์ส',
        description: 'รายละเอียดคอร์ส',
        duration: '12 ชั่วโมง',
        price: '2,500 บาท',
        image: 'URL รูปภาพ',
        level: 'ระดับ',
        color: 'from-blue-500 to-cyan-500' // Tailwind gradient
    },
    // เพิ่มคอร์สอื่นๆ...
];
```

### เปลี่ยนสี Theme

แก้ไข class ของ Tailwind CSS ใน `index.html`:
- สีหลัก: `indigo-600`, `indigo-500`
- Gradient: `from-blue-50 to-indigo-100`

### เพิ่มเวลาเรียน

แก้ไขใน `index.html` ที่ `<select id="bookingTime">`:

```html
<option value="เวลา">เวลาที่ต้องการ</option>
```

## 🔧 การแก้ไขปัญหา

### ปัญหา: ข้อมูลไม่บันทึกลง Google Sheets

1. ตรวจสอบว่า URL ของ Apps Script ถูกต้อง
2. ตรวจสอบว่า Deploy แบบ "Anyone" can access
3. ลอง Re-deploy ใหม่ด้วย Version ใหม่

### ปัญหา: CORS Error

- Google Apps Script ใช้ `mode: 'no-cors'` ในการ fetch
- ข้อมูลจะยังส่งได้ แต่จะไม่ได้ response กลับมา

### ปัญหา: GitHub Pages ไม่แสดงเว็บ

1. รอ 2-5 นาทีหลังจาก Push
2. ตรวจสอบว่าไฟล์ `index.html` อยู่ที่ root
3. ตรวจสอบ Settings → Pages ว่าเปิดใช้งานแล้ว

## 📱 Screenshot

หน้าเว็บจะแสดง:
- Section คอร์สเรียนแบบการ์ด 3 คอลัมน์
- ฟอร์มจองเรียนพร้อมช่องกรอกข้อมูล
- ปฏิทินแบบเดือน พร้อมแสดงวันที่มีการจอง

## 📄 License

Free to use for educational purposes.

---

Made with ❤️ using HTML, JavaScript & Tailwind CSS
